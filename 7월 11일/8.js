// 함수
function function_name(param1, param2) {
  // 코드
  return; //반환값
}

function sum(num1, num2) {
  return num1 + num2;
}
console.log(sum(3, 4));

// 회원가입
{
  // 1. 사용자의 입력 회원정보 -> 유효성 검사
  입력유효성검사();
  // 2. 사용자 중복 체크
  사용자중복체크();
  // 3. 사용자 DB 추가
  데이터베이스등록();
  // 4. 사용자 이메일 전송
  가입이메일전송();
}
